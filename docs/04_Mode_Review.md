# 📝 04_Mode_Review.md (复习模式)

**定位：** 记忆保持 (Retention)
**核心隐喻：** 完形填空试卷 (The Cloze Test)
**主题色：** Burnt Orange (`#E17055`)

---

## 1. 核心理念 (Philosophy)

**"Testing Effect" (测试效应)**
阅读后的测试比重复阅读更能强化记忆。Review 模式不是简单的单词卡片，而是将原文变成试卷，利用 **Active Recall (主动回忆)** 修复记忆链条。

---

## 2. 挖空算法 (The Cloze Algorithm)

系统如何决定挖空哪些词？

*   **Priority 1 (最高): Wrong/Clicked List**
    *   用户在 **Learn Mode** 中点击过（翻开过）的词。
    *   *逻辑:* "你刚才查了，说明你不懂，现在我要考考你。"
*   **Priority 2: Vocab Level Core**
    *   该 **Proficiency Level (A1-C2)** 对应大纲中的核心词汇。
*   **Priority 3: Syntax Anchors**
    *   关键语法词 (如 `However`, `Although`, `Despite`)，用于训练逻辑感。

---

## 3. 左侧阅读区交互 (The Quiz Stream)

### 3.1 视觉形态：Blur & Gap
*   **被选中的词:**
    *   文本被替换为模糊块 (`filter: blur(4px)`) 或 下划线 `_______`。
    *   颜色：橙色微光。

### 3.2 交互逻辑
*   **Hover (偷看):**
    *   鼠标悬停在模糊块上，模糊度降低 (`blur(0px)`)，显示原词。
    *   **Penalty:** 计入 "Peek Count" (偷看次数)，影响最终评分。
*   **Input (输入 - 可选高级功能):**
    *   支持直接键盘输入单词拼写。
*   **Click (确认掌握):**
    *   点击模糊块 -> 变为绿色常亮 -> 标记为 "Mastered"。

---

## 4. 右侧仪表盘：Stats Panel (统计面板)

Review 模式没有 Knowledge Hub，只有数据反馈。

### 4.1 模块一：Progress (进度)
*   **Recall Rate (回忆率):** `(Total Blurs - Peeks) / Total Blurs`.
*   **Visual:** 环形进度条。

### 4.2 模块二：Spaced Repetition (间隔重复)
*   显示本次复习对艾宾浩斯遗忘曲线的影响。
*   *文案示例:* "Great! You've pushed the forgetting curve for 'Universally' to 7 days."

### 4.3 模块三：Challenge (挑战)
*   **Combo System:** 连续正确拼写/回忆单词，触发 Combo 特效（火焰 UI）。

---

## 5. 退出机制
*   用户完成当前章节的 Review 后，系统弹出一个 **"Chapter Mastery"** 结算画面（S/A/B/C 评分）。
*   自动切回 **Flow Mode**，准备进入下一章。