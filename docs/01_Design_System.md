# 🎨 01_Global_Design_System.md (全局设计规范)

**适用范围：** 全平台 (Desktop/Mobile)
**核心原则：** 沉浸 (Immersive)、语义化 (Semantic)、极简 (Minimalist)

---

## 1. 布局框架 (Layout Framework)

### 1.1 桌面端 (Split Screen)
采用 **6:4 分屏** 结构。
*   **Left (60%): 阅读流 (Reader Stream)。**
    *   承载正文。
    *   背景色：`var(--bg-paper)`。
    *   排版重点：衬线体，大行距，最佳阅读宽度 (65ch)。
*   **Right (40%): 智能仪表盘 (Smart Dashboard)。**
    *   承载辅助信息 (Context/Knowledge/Stats)。
    *   背景色：`var(--bg-dashboard)`。
    *   视觉层级：略低于左侧，作为“伴随者”存在。

### 1.2 移动端 (Bottom Sheet)
*   **正文：** 全屏显示。
*   **仪表盘：** 也是一个可拖拽的 **Bottom Sheet (底部抽屉)**。
    *   *Collapsed:* 仅显示当前段落的 AI Insight 摘要。
    *   *Expanded:* 展开显示完整的 Knowledge Hub。

---

## 2. 视觉组件规范 (Visual Components)

### 2.1 视觉锚点：静默磁吸光标 (Silent Magnetic Marker)
这是贯穿全产品的核心交互组件，用于指示“当前关注的段落”。

*   **形态：** 左侧边缘 (`left: 0`) 的垂直细条，宽度 `3px`，圆角。
*   **颜色：** 跟随当前模式的主题色。
*   **交互逻辑：**
    *   **Idle (静止):** `opacity: 0.3`。
    *   **Scrolling (滚动中):** 跟随视窗中心段落，平滑过渡 (`transition: top 0.4s`)。
    *   **Active (阅读受阻/停留 > 5s):** `opacity: 1`，并带有轻微的呼吸动效 (`box-shadow` 扩散)，提示“AI 已就绪”。

### 2.2 顶部控制栏 (Control Bar)
*   **Mode Switcher (模式切换):**
    *   三段式 Toggle。
    *   图标：☕️ Flow | 🎓 Learn | 📝 Review。
    *   点击切换时，触发全局 CSS Variable 换肤。
*   **Level Slider (等级滑块):**
    *   5 档位。
    *   带有物理吸附感 (Snapping)。
    *   拖动时实时显示等级名称 (如 "Lv 3: Intermediate")。

---

## 3. 主题色系统 (Color System)

我们要利用颜色在潜意识层面引导用户的心智模式。

### 3.1 模式主题色 (Mode Themes)

| 变量名 | ☕️ Flow (心流) | 🎓 Learn (学习) | 📝 Review (复习) |
| :--- | :--- | :--- | :--- |
| `--theme-primary` | **Emerald Green** `#00B894` | **Royal Blue** `#2980B9` | **Burnt Orange** `#E17055` |
| `--theme-bg-light`| `#F0FDF4` (淡绿) | `#EAF2F8` (淡蓝) | `#FFF3E0` (淡橙) |
| `--highlight-color`| `rgba(0, 184, 148, 0.2)` | `rgba(41, 128, 185, 0.15)` | `rgba(225, 112, 85, 0.2)` |
| `--cursor-color` | `#D4AF37` (金色) | `#BDC3C7` (银灰) | `#E17055` (橙色) |

### 3.2 知识可视化色 (Data Visualization Colors)
用于 Learn 模式下的 **X-Ray** 视觉系统。

*   **Vocab (词汇):** `--color-vocab: #2980B9` (Blue) - 理性，积木。
*   **Syntax (句法):** `--color-syntax: #E67E22` (Orange) - 结构，逻辑。
*   **Culture (文化):** `--color-culture: #8E44AD` (Purple) - 深邃，历史。

---

## 4. 字体排印 (Typography)

*   **阅读区 (Serif):** 追求纸质书体验。
    *   Font Family: `'Crimson Pro'`, `'Georgia'`, Serif.
    *   Size: 1.25rem ~ 1.5rem.
    *   Line-height: 1.8.
*   **UI区 (Sans-Serif):** 追求信息清晰度。
    *   Font Family: `'Inter'`, `'San Francisco'`, System UI.
    *   Size: 0.85rem ~ 1rem.

---

## 5. 交互物理特性 (Interaction Physics)

*   **Hover Delay:**
    *   Flow Mode: `0ms` (即时响应，无摩擦)。
    *   Learn Mode: `150ms` (防误触，增加沉浸感)。
*   **Transitions:**
    *   所有主题色切换：`transition: all 0.5s ease` (缓慢柔和，不闪瞎眼)。
    *   卡片展开/Dashboard 滑出：`cubic-bezier(0.2, 0.8, 0.2, 1)` (富有弹性的原生感)。